<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

   <ul class="users">
         <!-- moved out the old, now we render -->
         <%= render @users %>
   </ul>

<%= will_paginate %>
